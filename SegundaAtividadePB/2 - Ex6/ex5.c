#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <math.h>
#include <string.h>
#include <locale.h>

int main() {
    setlocale(LC_ALL, "PORTUGUESE");

    int flagStop = 0;
    while (flagStop == 0)
    {
        if(flagStop == 0){
            int gen, olhos, cabelos, idade;
            printf("Digite 1 para masculino e 2 para feminio");
            scanf("%d", gen);

            printf("Digite 1 para azul e 2 para verdes 3 para castanhos");
            scanf("%d", olhos);

            printf("Digite 1 para louros, 2 para castanhos, 3 para pretos");
            scanf("%d", olhos);

        }
    }
    
}
